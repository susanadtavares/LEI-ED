
int main() {

    // Aqui só um exemplo. Depois completas com os teus testes.

    int M = 3, N = 3;
    int mat[] = {0,0,0, 1,0,0, 0,0,0};

    printf("Esparsa? %d\n", Esparsa(mat, M, N, 70));

    return 0;
}
